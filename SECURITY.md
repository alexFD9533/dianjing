# 安全政策

## 报告安全问题

请不要在公开 Issue 中提交密钥、真实页面内容、个人数据或可直接利用的安全细节。请优先使用 GitHub 的 Private Vulnerability Reporting；如果该入口不可用，可以发送邮件至 [280845201@qq.com](mailto:280845201@qq.com)，并在主题中注明“点睛安全问题”。

报告中请尽量包含：

- 浏览器、扩展版本和操作系统；
- 可复现的最小页面或脱敏 HTML；
- 触发步骤和实际影响；
- 是否涉及页面内容外传、越权访问、任意脚本执行或权限扩大。

## 当前安全边界

- 扩展默认使用 activeTab 和用户主动触发的访问授权；
- 普通网页编辑使用当前标签页的副本语义，不承诺回写原网站；
- 页面修改记录和工作台设置保存在浏览器本地会话/本地存储；
- 导出功能会读取页面资源并在本地生成静态结果；
- 点睛不提供服务器端上传、账号系统或内置外部 Agent 执行；
- http://_/_ 和 https://_/_ 是可选主机权限，只有用户主动进入相关网页编辑流程时才应请求。

安全边界以代码和 docs/open-source/permissions.md 为准；如果文档与运行行为不一致，请按更严格的边界处理并提交 Issue。
